import React from 'react'
import 'bootstrap/dist/css/bootstrap-reboot.min.css'

const Reboot = ({children}) => {
    return (
        <div>
            {children}
        </div>
    )
}

export default Reboot
